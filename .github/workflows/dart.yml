name: iOS-ipa-build

on:
  workflow_dispatch:

jobs:
  build-ios:
    name: 🎉 iOS Build
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          architecture: x64

      - name: Flutter pub get
        run: flutter pub get

      - name: Update CocoaPods repo
        run: |
          pod repo update
        working-directory: ios

      - name: Decode Provisioning Profile and Certificate
        run: |
          echo "$DEVELOPMENT_MOBILEPROVISION" | base64 --decode --output ./ios/Runner/DoMa_Development.mobileprovision
          echo "$IOS_DEVELOPMENT_CERT" | base64 --decode --output ./ios/Runner/ios_development.p12
<<<<<<< HEAD
          # # Import the certificate into the keychain
          # security create-keychain -p "" build.keychain
          # security import ./ios/Runner/ios_development.p12 -k build.keychain -P ${{ secrets.IOS_CERT_PASSWORD }} -T /usr/bin/codesign
          # security list-keychains -s build.keychain
          # security unlock-keychain -p "" build.keychain
          # security set-key-partition-list -S apple-tool:,apple: -s -k "" build.keychain

=======
          
>>>>>>> 1206a1549ea2a8f1dd7f5840a343ff4565669974
      - name: Build iOS Release
        run: flutter build ios --release

      - name: Move Runner.app to Payload
        run: |
          mkdir -p Payload
          mv ./build/ios/iphoneos/Runner.app Payload
        working-directory: .

      - name: Zip output
        run: zip -qq -r -9 FlutterIpaExport.ipa Payload
        working-directory: .

      - name: Upload binaries to release
        uses: svenstaro/upload-release-action@v2
        with:
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          file: ./FlutterIpaExport.ipa
          tag: v1.0
          overwrite: true
          body: "This is the first release"
